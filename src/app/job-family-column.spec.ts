import {Band} from './Band';
import {JobFamily} from './JobFamily';
import {DataService} from './data.service';
import {Capability} from './Capability';
import {Role} from './Role';
import { JobFamilyColumn } from './job-family-column';

describe('BandColumn', () => {
  it('should create an instance', () => {
    expect(new JobFamilyColumn()).toBeTruthy();
  });

  it('should have the correct capabilities as children', () => {

  	new JobFamily = 
  	new JobFamilyColumn() = testJobFamilyColumn;
  	new DataService() = testDataService;
  	testJobFamilyColumn.populateChildren(testDataService);

  	expect()
  })
});
